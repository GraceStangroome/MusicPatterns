// Fetching the data
class interpretation(data); {
  for(int i = 0; i<data.length; i++) {
    // println(data[i]);
    if(i <= -150);
      fill(0, 197, 255);
      stroke(255, 0);
    } else if(-149 < i < -100) { 
      ellipse(Math.abs(i), random(100), 3,3);
    } else if (-99 < i < -50) {
  }
} //<>//
